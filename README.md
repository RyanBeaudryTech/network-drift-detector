Network Drift Detector

A Python based network automation tool that detects configuration drift across network devices by comparing structured snapshots over time.

This project focuses on identifying unintended or unexpected changes in network state such as interfaces, routing, and ARP using structured data rather than raw CLI text diffs.